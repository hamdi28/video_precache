name: video_precache
description: A Flutter package for preloading and caching video URLs from Firebase Storage.
version: 0.0.1
homepage: https://github.com/hamdi28/video_precache

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_storage: 11.0.7
  get_it: ^7.2.0 # Optional, for dependency injection

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.0.0